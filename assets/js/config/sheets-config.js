// Google Sheets Configuration (legacy stub)
// Published sheet and API URL have been removed. All data is loaded via the
// Google Apps Script Web App (see app-config.js apiUrl).

const SheetsConfig = {
  getSheetUrl: function() {
    return null;
  },
  getSheetUrlByGid: function() {
    return null;
  }
};
